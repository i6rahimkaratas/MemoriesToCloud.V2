{
  "name": "photo-uploader",
  "version": "1.0.0",
  "description": "Kolay fotoğraf yükleme uygulaması",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/ && echo 'Build completed'",
    "start": "vercel dev"
  },
  "dependencies": {
    "cloudinary": "^1.41.0"
  },
  "engines": {
    "node": "22.x"
  }
}
